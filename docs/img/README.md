# Docs screenshots

Drop the real flow screenshots here, then replace the placeholder
`<div class="shot">` boxes in `../index.html` (search for "SCREENSHOTS")
with `<img src="img/<name>.png" ...>`.

Expected files:
- `flow-1-trigger.png` — the PR comment + check with the fix button / checkbox
- `flow-2-onit.png`    — the "🔧 Cyclops is on it" status comment (or suggested diff)
- `flow-3-green.png`   — the "✅ fixed it — CI is green" result on a green PR
