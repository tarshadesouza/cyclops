---
phase: 04-public-sdk
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - packages/core/package.json
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "publint runs against @cyclops/core and reports zero errors"
    - "@arethetypeswrong/cli runs against @cyclops/core and reports zero errors"
    - "CI fails the build if either publint or attw reports an error"
    - "The validate-sdk gate runs on every push and PR to main"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "validate-sdk job running publint + attw as a blocking gate"
      contains: "publint"
    - path: "packages/core/package.json"
      provides: "lint:publish script invoking publint + attw"
      contains: "attw"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "@arethetypeswrong/cli"
      via: "validate-sdk job step"
      pattern: "attw|arethetypeswrong"
    - from: ".github/workflows/ci.yml validate-sdk"
      to: "build job"
      via: "needs: build"
      pattern: "needs:"
---

<objective>
Add `publint` and `@arethetypeswrong/cli` (attw) validation of the built `@cyclops/core` package and wire them into `.github/workflows/ci.yml` as a blocking gate that runs before any publish is possible.

Purpose: SDK-04 requires publint + attw validation in CI on every publish candidate, with zero errors (Success Criterion 4). This catches malformed exports maps, missing files, and ESM/CJS type resolution bugs before they reach npm.
Output: A `validate-sdk` CI job (needs: build) that builds core, runs publint and attw, and fails the workflow on any error.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-sdk/04-RESEARCH.md
@.planning/phases/04-public-sdk/04-03-SUMMARY.md

# Current .github/workflows/ci.yml has a single `build` job: checkout → pnpm setup →
# setup-node(22, cache pnpm) → pnpm install --frozen-lockfile → pnpm build → tsc --build --noEmit.
# attw against a dual ESM/CJS tsup package should pass; if a known-acceptable condition needs
# suppressing, use an explicit --ignore-rules flag rather than a blanket pass.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add publint + attw scripts to @cyclops/core</name>
  <files>packages/core/package.json</files>
  <action>
    1. Add devDependencies to packages/core: `publint` and `@arethetypeswrong/cli`. Run `pnpm install`.
    2. Add scripts to packages/core/package.json:
       - `"lint:publish": "publint --strict && attw --pack ."`
       (publint --strict treats warnings as errors; attw --pack runs against the packed tarball,
       exactly what npm would publish. If attw flags a false-positive for the dual-format setup,
       narrow with `attw --pack --ignore-rules <specific-rule>` and document why in the SUMMARY —
       do NOT broadly disable.)
    3. Verify locally: `pnpm --filter @cyclops/core build && pnpm --filter @cyclops/core run lint:publish`
       must exit 0 with zero errors.
  </action>
  <verify>`pnpm --filter @cyclops/core run lint:publish` exits 0; publint reports no problems; attw reports all resolutions OK.</verify>
  <done>@cyclops/core has a lint:publish script that runs publint --strict + attw --pack with zero errors locally.</done>
</task>

<task type="auto">
  <name>Task 2: Wire validate-sdk gate into ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add a new `validate-sdk` job to .github/workflows/ci.yml that runs on the same triggers
    (push + PR to main) and gates on the build:
    - `needs: build`
    - steps: actions/checkout@v4 → pnpm/action-setup@v4 (version 9) →
      actions/setup-node@v4 (node 22, cache pnpm) → `pnpm install --frozen-lockfile` →
      `pnpm --filter @cyclops/core build` → `pnpm --filter @cyclops/core run lint:publish`
    The job MUST fail the workflow if publint or attw returns a non-zero exit code (default behavior
    of `run:` steps — do not add `continue-on-error`). This is the blocking gate before publish.
    Keep the existing `build` job unchanged.
  </action>
  <verify>
    `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` parses without error.
    `grep -n 'validate-sdk\|publint\|lint:publish\|needs: build' .github/workflows/ci.yml` shows the new job wired with a build dependency.
  </verify>
  <done>ci.yml has a validate-sdk job (needs: build) that runs publint + attw as a hard, non-bypassable gate on every push/PR to main.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @cyclops/core run lint:publish` exits 0 with zero publint and zero attw errors (Success Criterion 4)
- ci.yml is valid YAML and contains a validate-sdk job depending on build
- The gate uses default fail-on-error (no continue-on-error)
</verification>

<success_criteria>
- publint + attw run against @cyclops/core in CI (SDK-04)
- Zero errors reported on the current publish candidate
- CI blocks on any validation failure before publish is possible
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-sdk/04-04-SUMMARY.md`
</output>
